# Cluster entry
server.id=3
server.name=Bono
server.host=serv-bono.strik.it
server.port=13381

# MongoDB cluster 
db.server=localhost
db.name=strik

# Path to locale directory (either relative/absolute)
locale.dir=locale

# Facebook reward system
facebook.page.id=1337