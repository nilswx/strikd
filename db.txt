// One big MongoDB database (can shard)

// Player collection
var player = 
{
	// Identity
	id : Int64,
	name : String,

	// Player
	joined : Date,
	lang : String,
	xp : Int32,
	items : Array,
	avatar : 
	{
		// key/value object (avatar part key, part ID)
	}

	// Facebook (+ OAuth)
	fbUserId : Int64,
	fbToken : String,
	fbSecret: String,
	fbCountry : String
}

// Online instances collection
var instances = 
{
	name : String,
	host : String, // hostname
	port : Integer, // default port
	version : String,
	started : Date, // DateTime (can calculate uptime!)
	
	memory : Integer, // in MiB
	logins : Integer,
	onlinePlayers : Integer,
	activeMatches : Integer,
	totalMatches : Integer,
	avgWaitingTime : Integer, // seconds
}